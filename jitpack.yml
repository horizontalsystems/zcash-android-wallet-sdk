jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.14-amzn
  - sdk use java 17.0.14-amzn
install:
  - ./gradlew :sdk-lib:publishToMavenLocal :backend-lib:publishToMavenLocal :lightwallet-client-lib:publishToMavenLocal :sdk-incubator-lib:publishToMavenLocal -PskipCargoBuild=true
